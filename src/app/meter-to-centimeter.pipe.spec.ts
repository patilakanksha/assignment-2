import { MeterToCentimeterPipe } from './meter-to-centimeter.pipe';

describe('MeterToCentimeterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeterToCentimeterPipe();
    expect(pipe).toBeTruthy();
  });
});
